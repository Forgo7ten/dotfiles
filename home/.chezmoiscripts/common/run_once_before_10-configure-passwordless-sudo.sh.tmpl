{{- /* 仅在 macOS，或非 server 的 Linux 系统中执行。
为当前用户写入 /etc/sudoers.d/<用户名>，授予免密码执行 sudo 的权限。 */ -}}
{{- if or
      (eq .chezmoi.os "darwin")
      (and (eq .chezmoi.os "linux") (ne .system "server"))
-}}
{{ include "../install/common/configure-passwordless-sudo.sh" }}
{{- end }}
